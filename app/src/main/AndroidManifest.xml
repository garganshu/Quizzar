<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="zersey.com.zersey">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUp" />
        <activity
            android:name=".Home_Navigation"
            android:label="@string/title_activity_home__navigation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Create_Quiz" />
        <activity android:name=".Quiz_One" />
        <activity android:name=".Quiz_Two"></activity>
    </application>

</manifest>